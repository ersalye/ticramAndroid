package com.turnpoint.ticram.Services;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

import com.turnpoint.ticram.Activites.TripDetails;

public class BroadcastReceiver_LocUpdate extends BroadcastReceiver {

    @Override
    public void onReceive(Context context, Intent intent) {
        // TODO: This method is called when the BroadcastReceiver is receiving

       /* Intent i = new Intent(context, TripDetails.Service_getDriverLoc.class);
        context.startService(i);*/
    }
}
